# API base URL (proxied through Next.js rewrites in dev)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Enable MSW mock API in the browser (off by default — use live backend)
NEXT_PUBLIC_ENABLE_MSW=false
